spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://10.0.1.10:3306/quiz_database?serverTimezone=UTC&characterEncoding=UTF-8
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update  # (or validate, none, create-drop, create)
    show-sql: true
    properties:
      hibernate:
        format_sql: true
#  sql:
#    init:
#        mode: always  # 애플리케이션 실행 시 SQL 스크립트 실행

